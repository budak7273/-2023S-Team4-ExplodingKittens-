package system.cards;

import datasource.CardType;
import system.Card;

public class HairyPotatoCatCard extends Card {
    public HairyPotatoCatCard() {
        super(CardType.HAIRY_POTATO_CAT);
    }
}
